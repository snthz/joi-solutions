---
import Layout from "../layouts/Layout.astro";
import { Image } from "astro:assets";
import heroBg from "../assets/hero-bg.svg";
import Benefits from "../components/Benefits.astro";
import QuoteAnimation from "../components/QuoteAnimation.astro";
---

<Layout title="Contacto" description="Conversemos y construyamos el futuro juntos. Contáctanos para impulsar tu proyecto con tecnología innovadora.">
  <section class="relative overflow-hidden">
    <div class="absolute inset-0 -z-10 pointer-events-none" aria-hidden="true">
      <div class="absolute inset-0 opacity-40" style="background-image: linear-gradient(rgba(32,57,111,0.035) 1px, transparent 1px), linear-gradient(90deg, rgba(32,57,111,0.035) 1px, transparent 1px); background-size: 80px 80px;"></div>
    </div>
    <div
      class="w-full flex flex-col items-center pt-36 overflow-hidden justify-center px-4 md:pl-2 relative h-[70vh] md:min-h-[80vh] 3xl:h-[10vh]"
    >
      <span
        class="border border-neutral-200 px-6 py-1 rounded-full text-center text-neutral-500 text-lg mb-6 font-medium animate-button bg-white cursor-trigger cursor-pointer"
      >
        <span class="flex items-center gap-1">Hablemos</span>
      </span>

      <!-- (Arregla el doble cierre de </small>) -->
      <div
        class="text-[2em] leading-[1.3em] xl:text-6xl 3xl:text-[5em] flex flex-col gap-2 items-center text-neutral-700 text-center text-balance"
      >
        <span class="font-medium animate-text">
          ¿Listo para <span class="text-tertiary font-bold">impulsar</span> tu proyecto?<span
            class="text-tertiary">.</span
          >
        </span>
        <small
          class="px-2 text-sm md:text-xl text-neutral-500 animate-text text-balance max-w-[80ch]"
        >
          Creemos en el poder de las ideas que transforman. Si tú también,
          hablemos y construyamos el futuro juntos<span class="text-tertiary"
            >.</span
          >
        </small>
      </div>
    </div>

    <div class="absolute inset-0 -z-10 pointer-events-none">
      <Image
        src={heroBg}
        alt="Background"
        class="w-full h-full object-cover opacity-50"
        loading="lazy"
        width={1920}
        height={1080}
        priority
      />
      <div
        class="absolute top-0 left-0 w-full h-full bg-[radial-gradient(ellipse_at_60%_40%,rgba(0,0,0,0)_0%,rgba(255,255,255,0.8)_60%)] pointer-events-none mix-blend-screen"
      >
      </div>
    </div>
  </section>

  <!-- FORM sin scroll interno; alto dinámico controlado por ClickUp -->
  <section id="contact-form" class="relative py-10 scroll-none">
    <div class="overflow-hidden">
      <iframe
        class="clickup-embed clickup-dynamic-height"
        src="https://forms.clickup.com/9011902119/f/8cjddn7-17351/B41PJZET1J51ZNXSTK"
        onwheel=""
        width="100%"
        height="100%"
        style="background: transparent; border: 1px solid #ccc;"
      ></iframe><script
        async
        src="https://app-cdn.clickup.com/assets/js/forms-embed/v1.js"></script>

      <!-- Fallback mínimo por si el script tarda en aplicar la altura -->
      <style is:global>
        .clickup-embed.clickup-dynamic-height {
          min-height: 150vh; /* ajusta si quieres menos/más espacio inicial */
          overflow: hidden;
        }
        .cu-form {
          overflow: hidden !important;
        }
        .inside-iframe {
          overflow: hidden !important;
        }
      </style>
    </div>
  </section>
</Layout>
